{
  "name": "i-have-money-app",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first PWA expense tracker (I Have Money). No build step — plain HTML/CSS/JS served by nginx.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
